{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/proxy?path=:path*"
    }
  ],
  "buildCommand": "npm run build",
  "env": {
    "API_BASE_URL": "@api_base_url"
  }
}
